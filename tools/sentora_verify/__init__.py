"""Sentora Audit Chain Verifier — air-gapped epoch verification tool.

Standalone CLI tool with zero external dependencies (stdlib only).
Verifies the integrity of exported audit epoch JSON files without
network access or any Sentora backend dependency.
"""

__version__ = "1.0.0"
