"""Enforcement rules domain.

Provides taxonomy-based software policy enforcement.  Rules define
which software must be present (required), must be absent (forbidden),
or must be the only software installed (allowlist) on scoped endpoints.
"""
